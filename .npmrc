# blobity@0.2.3 declares a peer dependency on `vue@next`, a dist-tag that no
# longer exists on npm. Strict peer resolution makes the whole install fail with
# ETARGET, so fall back to npm 6 behaviour and ignore peer deps.
legacy-peer-deps=true
